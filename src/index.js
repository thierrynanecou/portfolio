import React from 'react';
import ReactDOM from 'react-dom/client';
import ConfigRouter from './configRouter/configRouter';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ConfigRouter />
  </React.StrictMode>
);


